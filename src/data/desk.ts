/**
 * The two demo personas the dashboard speaks for, and the worked example the
 * supplier's plan card is built on (live dashboards, 23 Sep). They lived inside
 * Dashboard.tsx until the SVS desk, the analytics screen and the deck needed
 * the same names; one home keeps every surface pointing at the same people.
 */

/** The demo client. Fictional operator, like everything else here (03 §3.4). */
export const DEMO_CLIENT = 'Browne Energy';

/** The demo supplier whose side of the platform the supplier view shows. */
export const DEMO_SUPPLIER_ID = 'silver-city-welding';

/** The job value the supplier's keep-more example is worked on — the crane
 *  quote the rest of the demo settles on, so the two figures agree. */
export const EXAMPLE_JOB_GBP = 4400;
